# Repository Working Rules

- Do not create or use Git worktrees for this repository unless the user explicitly authorizes it in the current conversation.
- Make changes on a local branch checked out directly in `D:\WorkSpace\Zero`, so the root development server can preview them immediately.
- Preserve existing user changes when switching branches; never discard or overwrite unrelated work.
